# ModalChat 插件 — 项目概述与需求分析

> 编写日期：2026年7月25日

## 一、项目背景

ModalChat（多模态聊天）是 chat-images 和 MatchScoring 两个 SillyTavern 第三方插件的融合升级版本。

### 1.1 融合动机

| 方面 | chat-images | MatchScoring | 融合后 |
|------|-------------|-------------|--------|
| 核心功能 | 正则匹配插入图片 | NLP 评分匹配媒体文件 | 两者兼具 |
| 文件管理 | 规则内嵌图片上传 | 独立文件 CRUD | 统一文件管理器 |
| 配置体系 | 内嵌在规则设置 | 配置界面 + 通用配置 | 整合为通用配置 |
| 数据存储 | extensionSettings | extensionSettings | 统一数据层 |

### 1.2 设计目标

1. **统一入口** — 一个导航栏入口，四个标签页
2. **统一文件管理** — 独立文件管理器，所有模块通过文件注册表引用文件
3. **保留各自逻辑** — chat-images 匹配引擎和 MatchScoring 评分引擎各自独立
4. **自动迁移** — 首次启动时自动导入旧插件数据
5. **兼容保留** — 旧插件文件夹保留作为备份

---

## 二、导航栏布局

```
┌─ 多模态聊天 ──────────────────────────────────────┐
│ [聊天图片] [匹配打分] [文件管理] [通用配置]         │  ← 一级标签
├────────────────────────────────────────────────────┤
│                                                     │
│  聊天图片标签页：                                    │
│   ┌──────────────────────────────┐                  │
│   │ [角色集] [规则集] [规则]     │  ← 二级标签      │
│   └──────────────────────────────┘                  │
│                                                     │
│  匹配打分标签页：                                    │
│   ┌──────────────────────────────┐                  │
│   │ [文件展示] [匹配配置]        │  ← 二级标签      │
│   └──────────────────────────────┘                  │
│                                                     │
│  文件管理标签页：                                    │
│   目录树 + 文件列表 + 操作工具栏                     │
│                                                     │
│  通用配置标签页：                                    │
│   图片/视频尺寸 + 背景设置 + 其他                    │
└─────────────────────────────────────────────────────┘
```

### 2.1 标签页职责

| 标签页 | 二级导航 | 职责来源 |
|--------|---------|---------|
| 聊天图片 | 角色集 / 规则集 / 规则 | chat-images |
| 匹配打分 | 文件展示 / 匹配配置 | MatchScoring |
| 文件管理 | —（无二级标签） | **新增** |
| 通用配置 | —（无二级标签） | chat-images 设置 + MatchScoring 通用配置 |

---

## 三、关键设计决策

### 3.1 文件引用模型

chat-images 和 MatchScoring **不再直接操作服务端文件**，所有文件操作委托给文件管理器：

```
用户上传文件
      │
      ▼
┌──────────────────┐
│  文件管理器       │  → 上传到服务器（生成唯一标识文件名）
│  (唯一负责文件IO)  │  → 写入文件注册表 { id, 真实文件名, 展示名, 路径, ... }
└──────────────────┘
      │
      ▼
┌──────────────────┐
│  文件注册表       │  → chat-images 规则引用注册表 ID
│  (中央索引)       │  → MatchScoring 匹配引用注册表 ID
└──────────────────┘
```

### 3.2 唯一文件名格式

- chat-images 源文件：`ChatImages_YYYYMMDD_HHmmss_XXXXXX.ext`
- MatchScoring 源文件：`MatchScoring_YYYYMMDD_HHmmss_XXXXXX.ext`
- 文件管理器上传：`MM_YYYYMMDD_HHmmss_XXXXXX.ext`

### 3.3 文件存储路径

所有文件存储在：`data/default-user/user/files/` 下，按目录组织。

### 3.4 旧插件兼容

- 首次启动时扫描旧插件数据，自动导入到文件注册表
- 旧插件文件夹保留，不删除
- 旧插件的已上传文件路径保持不变，注册表直接引用

---

## 四、与旧插件的差异

| 方面 | 旧插件 | ModalChat |
|------|--------|-----------|
| 文件上传 | 各自实现 | 统一由文件管理器处理 |
| 文件引用 | 直接存储文件路径 | 通过文件注册表 ID 引用 |
| 批量操作 | 各自实现 | 统一在文件管理器中 |
| 导航栏 | 各自独立图标 | 单个图标 + 标签页 |
| 数据存储 | 各自 settings 键 | 统一 `modal-chat` 键 |
