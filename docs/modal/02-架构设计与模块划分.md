# ModalChat 插件 — 架构设计与模块划分

> 编写日期：2026年7月25日 | 最后更新：2026年7月27日

## 一、项目结构

```
ModalChat/
├── manifest.json                  # 插件清单
├── index.js                       # ★ 入口：生命周期、导航栏创建、事件注册
├── data.js                        # ★ 全局数据层（多文件存储）
├── style.css                      # 全局样式
├── settings.html                  # 扩展设置模板
│
├── shared/                        # 共享模块
│   ├── utils.js                   # 工具函数
│   ├── file-service.js            # ★ 文件服务（上传/删除/移动/重命名/目录CRUD）
│   ├── file-enlarge.js            # 全屏放大查看器（图片/视频/音频）
│   ├── file-icons.js              # 文件图标和类型标签
│   ├── popup-type.js              # 弹窗类型常量
│   ├── bounce-editor/             # 果冻弹跳预设可视化编辑器
│   │   ├── index.js               # 编辑器主类（BounceEditor）
│   │   ├── render.js              # 渲染管线
│   │   └── interaction.js         # 交互事件处理
│   └── image-drag-physics/        # 图片拖拽变形物理引擎
│       ├── index.js               # 导出 hub
│       ├── drag-interaction.js    # 拖拽交互
│       ├── combined-engine.js     # 合批弹跳引擎
│       ├── bounce-engine.js       # 基础弹跳引擎
│       ├── displacement-maps.js   # 置换贴图预计算
│       └── svg-utils.js           # SVG 工具函数
│
├── nav-chat-images/               # 「聊天图片」标签页
│   ├── index.js                   # 薄导入层（导出 renderChatImages, bindChatImagesEvents）
│   ├── data.js                    # 规则/规则集/角色集 CRUD（引用注册表 ID）
│   ├── rules-ui.js                # ★ 规则界面 UI + 子导航栏（角色集/规则集/规则）
│   └── matcher.js                 # ★ 正则匹配引擎（加权随机、图片队列轮播）
│
├── nav-match-scoring/             # 「匹配打分」标签页
│   ├── index.js                   # 薄导入层（导出 renderMatchScoring, bindMatchScoringEvents）
│   ├── drawer.js                  # ★ 子导航栏渲染（文件展示/匹配配置/轮播配置）
│   ├── file-display.js            # ★ 文件展示 UI（上传/筛选/排序/批量操作）
│   ├── match-config.js            # ★ 匹配配置 UI（权重滑条/存档管理/停用词/关联词）
│   ├── scorer.js                  # ★ 评分引擎（N-gram 分词/多维加权/Top-N）
│   ├── carousel-config.js         # 轮播插位配置（插位槽/类型/时长/动画）
│   ├── match-chat-results.js      # ★ 匹配结果聊天气泡渲染与持久化
│   ├── popup-config.js            # 配置弹窗（关联词/停用词/关键词编辑/标签筛选）
│   └── popup-batch.js             # 批量新增弹窗（选择文件+设置共用属性）
│
├── nav-file-manager/              # 「文件管理」标签页（新增）
│   ├── index.js                   # 薄导入层（导出 renderFileManager, bindManagerEvents 等）
│   ├── file-render.js             # ★ 文件管理器渲染（目录树/文件列表/面包屑/分页）
│   ├── file-events.js             # ★ 事件绑定（上传/删除/移动/重命名/目录操作）
│   ├── file-state.js              # ★ 状态管理（currentDir/选中/分页/排序/搜索）
│   ├── file-url.js                # 根据注册表条目获取可访问 URL
│   ├── file-picker-popup.js       # 文件选择器弹窗（供其他模块调用）
│   ├── file-manager-ui.js         # 重新导出 hub
│   └── file-registry.js           # 注册表查询操作（搜索/类型/来源）
│
├── nav-general-settings/          # 「通用配置」标签页
│   ├── index.js                   # 薄导入层（导出 renderGeneralSettings, bindGeneralEvents）
│   ├── settings-ui.js             # ★ 子导航调度（UI配置/轮播类型/动态图预设）
│   ├── ui-config.js               # UI 配置子页（图片尺寸/媒体尺寸/背景颜色透明度）
│   ├── anim-types.js              # 轮播类型编辑子页（创建/编辑/删除 @keyframes）
│   └── bounce-presets/            # 动态图预设子页
│       ├── index.js               # 导出 hub
│       ├── bounce-presets.js      # ★ 核心逻辑（编辑器实例/渲染/事件）
│       ├── fullscreen.js          # 全屏画布编辑器
│       └── params.js              # 参数面板渲染与输入处理
│
└── docs/
    └── modal/                     # 本文档目录
```

## 二、模块依赖关系

```
                           ┌──────────────────────┐
                           │     index.js          │  (入口)
                           │  生命周期/导航栏/事件   │
                           └───────┬──────────────┘
                                   │
                    ┌──────────────┼──────────────────┐
                    │              │                    │
                    ▼              ▼                    │
           ┌──────────────┐  ┌──────────────────┐      │
           │   data.js    │  │  各标签页 index.js │      │
           │ (全局数据层)  │  │  (薄导入层)       │      │
           └───────┬──────┘  └──────────────────┘      │
                   │                                    │
         ┌─────────┼─────────┬──────────┬──────────┐    │
         │         │         │          │          │    │
         ▼         ▼         ▼          ▼          ▼    │
   ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ │
   │chat-     │ │match-    │ │file-     │ │general   │ │
   │images    │ │scoring   │ │manager   │ │settings  │ │
   └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘ │
        │            │            │            │        │
        └────────────┼────────────┼────────────┘        │
                     │            │                      │
                     ▼            ▼                      │
           ┌──────────────────────────────────────┐     │
           │           shared/                     │     │
           │  ┌──────────┐ ┌────────────────┐      │     │
           │  │ utils.js │ │ file-service.js │      │     │
           │  │file-     │ │ (文件操作)      │      │     │
           │  │enlarge   │ └────────────────┘      │     │
           │  │file-icons│                          │     │
           │  │popup-type│                          │     │
           │  └──────────┘                          │     │
           │  ┌──────────────┐ ┌──────────────────┐ │     │
           │  │bounce-editor │ │image-drag-physics │ │     │
           │  └──────────────┘ └──────────────────┘ │     │
           └────────────────────────────────────────┘     │
                                                          │
                    ┌─────────────────────────────────────┘
                    │
                    ▼
           ┌──────────────────┐
           │ SillyTavern API  │
           │ /api/files/*     │
           │ eventSource      │
           └──────────────────┘
```

## 三、模块职责详表

| 模块 | 职责 | 核心导出 |
|------|------|---------|
| `index.js` | 生命周期钩子、导航栏 Drawer 创建、4 个标签页切换调度、SillyTavern 事件监听/回调、状态缓存、debounce 保存 | `init`, `onDelete`, `onEnable`, `onDisable` |
| `data.js` | 多文件 JSON 存储（读写/防抖保存）、注册表按目录分文件懒加载、海量条目支持（5万+）、各模块数据访问器、虚拟目录管理、数据迁移补全 | `loadSettings`, `saveSettings`, `addRegistryEntry`, `getRegistryById`, `searchRegistry` 等 |
| `shared/utils.js` | ID 生成、HTML 转义、文件类型检测、图标映射、文件大小/日期格式化、防抖、服务端文件名生成、颜色转换 | `generateId`, `escapeHtml`, `detectFileType`, `formatFileSize`, `generateServerFilename` |
| `shared/file-service.js` | 服务端文件上传/删除/批量操作、注册表移动/重命名（逻辑）、目录创建/删除（含递归子目录清理） | `uploadFile`, `uploadFiles`, `deleteFile`, `deleteFiles`, `moveFile`, `renameFile`, `createDirectory`, `deleteDirectory` |
| `shared/file-enlarge.js` | 图片/视频/音频全屏放大查看器（含双指缩放、拖拽平移、事件冒泡防护） | `showFileEnlarge` |
| `shared/file-icons.js` | 文件图标映射表、类型标签/图标获取 | `getTypeLabel`, `getFileIcon` |
| `shared/popup-type.js` | SillyTavern 弹窗类型常量 | `POPUP_TYPE` |
| `shared/bounce-editor/` | BounceEditor 类：画布渲染、点/骨骼编辑、撤销栈、全屏编辑 | `BounceEditor` |
| `shared/image-drag-physics/` | 基于 SVG feDisplacementMap 的果冻弹跳物理引擎 | `setupDragDeformation`, `autoBounce` |
| `nav-chat-images/data.js` | 规则/规则集/角色集 CRUD、启用规则过滤、获取文件 URL | `getRules`, `addRule`, `updateRule`, `deleteRule`, `getEnabledRules` |
| `nav-chat-images/matcher.js` | 正则匹配引擎（增强版）：加权随机选择、按规则集分组、图片队列轮播、定时器管理 | `performMatch`, `matchSingleRule`, `clearAllImageTimers` |
| `nav-chat-images/rules-ui.js` | 聊天图片全部 UI（含子导航栏）：角色集/规则集/规则 列表渲染、CRUD 界面、图片选择弹窗联动 | `renderChatImages`, `bindChatImagesEvents` |
| `nav-match-scoring/drawer.js` | 匹配打分子导航渲染（文件展示/匹配配置/轮播配置 三个子标签页） | `renderMatchScoring`, `bindMatchScoringEvents` |
| `nav-match-scoring/file-display.js` | 文件列表展示（上传/批量新增/筛选/排序/分页/标签筛选/网格设置） | `renderFileDisplay`, `bindFileDisplayEvents` |
| `nav-match-scoring/match-config.js` | 匹配配置 UI：权重滑条、配置存档（新建/保存/加载/删除）、停用词/适配词/关联词、类型关键词 | `renderMatchConfig`, `bindMatchConfigEvents` |
| `nav-match-scoring/scorer.js` | 评分引擎：N-gram 分词、多维加权（来源/内容/标签/关联/停用词/字符模糊/类型偏好）、随机抖动、Top-N 过滤 | `scoreMessages`, `filterResultsForChat`, `cleanText`, `getLastResults` |
| `nav-match-scoring/carousel-config.js` | 轮播插位配置：多层插位、类型筛选、展示数量/时长/最低分、动画类型选择 | `renderCarouselConfig`, `bindCarouselEvents` |
| `nav-match-scoring/match-chat-results.js` | 匹配结果聊天气泡渲染（插位槽/动画/类型分组）、结果面板、持久化/恢复 | `renderResultsPanel`, `renderChatResults`, `persistChatResults`, `restoreChatResults` |
| `nav-match-scoring/popup-config.js` | 配置弹窗集合：关联词编辑、停用词编辑、类型关键词编辑、配置手册、标签筛选 | 弹窗函数 |
| `nav-match-scoring/popup-batch.js` | 批量新增弹窗：调用文件选择器、设置共用名称/标签/内容 | `showBatchAddPopup` |
| `nav-file-manager/file-render.js` | 文件管理器渲染：活动目标支持、目录树/文件列表/面包屑/分页/排序，支持 simple/detail 两种视图 | `renderFileManager`, `setActiveFileManagerTarget` |
| `nav-file-manager/file-events.js` | 文件管理器事件绑定（事件委托）：上传/删除/重命名/目录创建删除、文件详情弹窗、全选/反选 | `bindManagerEvents`, `enterSelectionMode` |
| `nav-file-manager/file-state.js` | 模块级状态变量：当前目录、选中集合、分页、排序、视图模式、搜索模式 | 状态变量 + setter 函数 |
| `nav-file-manager/file-url.js` | 根据注册表条目或 ID 获取可访问 URL | `getFileUrl` |
| `nav-file-manager/file-picker-popup.js` | 文件选择器弹窗（供其他模块调用）：全功能文件管理器界面，确认后返回选中条目 | `showFilePickerPopup` |
| `nav-file-manager/file-manager-ui.js` | 从各子模块重新导出公开接口 | 导出 hub |
| `nav-file-manager/file-registry.js` | 注册表查询操作：按来源/名称模糊搜索、获取所有类型/来源 | `searchRegistry`, `getAllTypes`, `getAllSources` |
| `nav-general-settings/settings-ui.js` | 通用配置子导航调度（UI配置/轮播类型/动态图预设） | `renderGeneralSettings`, `bindGeneralEvents` |
| `nav-general-settings/ui-config.js` | UI 配置子页：图片尺寸、媒体尺寸、背景颜色/透明度 | `renderUiConfig`, `bindUiEvents` |
| `nav-general-settings/anim-types.js` | 轮播类型编辑子页：创建/编辑/删除动画类型、@keyframes 编辑器、缓动函数 | `renderAnimTypes`, `bindAnimEvents` |
| `nav-general-settings/bounce-presets/` | 动态图预设子页：BounceEditor 集成、全屏编辑视图、复制/粘贴/撤销 | `renderBounceEditor`, `initBounceEditor`, `bindBounceEvents` |

## 四、数据流

```
                    ┌────────────┐
                    │ 导航栏图标  │
                    └──────┬─────┘
                           │ 点击展开 Drawer
                           ▼
                    ┌──────────────┐
                    │  switchTab() │
                    │  切换标签页   │
                    └──────┬───────┘
                           │
              ┌────────────┼────────────────┐
              │            │                │
              ▼            ▼                ▼
      ┌────────────┐ ┌────────────┐ ┌──────────────┐
      │ 导入对应    │ │ 渲染 UI    │ │ 绑定事件      │
      │ 模块的      │ │ renderXxx │ │ bindXxxEvents│
      │ render/bind │ └─────┬──────┘ └──────┬───────┘
      └────────────┘       │               │
                           ▼               ▼
                    ┌──────────────────────────┐
                    │     data.js              │
                    │   (数据持久化/读取)        │
                    └────────────┬─────────────┘
                                 │
                    ┌────────────┴─────────────┐
                    │    shared/file-service   │
                    │    (服务端文件操作)        │
                    └──────────────────────────┘
```

## 五、关于文件引用机制

聊天图片和匹配打分不再直接上传/删除服务端文件。当它们需要图片时：

1. **批量新增** → 调用 `showFilePickerPopup()` 弹出文件管理器选择界面 → 用户从已有文件中选择 → 保存文件注册表 ID
2. **展示媒体** → 通过注册表 ID 查询文件路径（`getRegistryById()` + `getFileUrl()`）→ 渲染
3. **删除规则/文件** → 仅删除元数据引用，不删除实际文件（文件管理器独立管理）
